{
  "buildpacks": [],
  "env": {
    "OPENTOK_EMBED_CODE": {
      "description": "The Video Embed code from your TokBox account",
      "required": true
    },
    "LOCK_SETUP": {
      "description": "Hide option to change OpenTok embed code once it has been set. Set to 'true' to enable lock",
      "value": "true",
      "required": false
    },
    "NODE_ENV": {
      "description": "Run as a production environment on Heroku",
      "value": "production",
      "required": false
    },
    "NODE_DEBUG": {
      "description": "Enable debug for modules",
      "value": "app",
      "required": false
    }
  },
  "formation": {},
  "name": "ot-embed-appointment-demo",
  "scripts": {}
}
